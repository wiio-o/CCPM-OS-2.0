-- CCPM-OS Settings

return {
    -- visual theme
    theme = {
        desktop = colors.cyan,
        window = colors.lightGray,
        titleActive = colors.blue,
        titleInactive = colors.gray,
        text = colors.black
    },

    -- behavior
    behavior = {
        enableSounds = false,
        doubleClickSpeed = 0.4,
        confirmShutdown = true
    },

    -- system info
    system = {
        name = "CCPM-OS",
        version = "0.1",
        codename = "Windows 3.1 Inspired"
    }
}
